body {
    font-family: Arial, sans-serif;
    background-color: #fff;
   align-items:center ;
   display: flex;
   justify-content: center;
    height: 100vh;
    margin: 0;
}
.singup-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    margin: 0 auto;
   
}
.singup-form-container {
   
    flex-direction: column;
    font-family: Arial, sans-serif; 
    margin: 20px;
    color: #022243;  
    width: 50%;
    margin: 100px;
     position: relative;

     
}
.login-image {
    width: 45%;
    text-align: right;
}


.login-image img {
    width: 100%;
     float: right;
     height:120vh;
}


.name-fields {
    display: flex;
    justify-content: space-between;
    gap: 20px; /* Augmente la distance entre les deux champs */
}

.name-fields div {
    width: 48%; /* Ajuste la largeur pour que les champs aient suffisamment d'espace */
}

#first-name,
#last-name {
    width: 100%; /* Assure que chaque champ occupe tout l'espace dans son conteneur parent */
    padding: 15px; /* Ajoute de l'espace à l'intérieur */
    border-radius: 10px;
    background-color: rgb(228, 225, 225);
    font-size: 16px; /* Taille cohérente */
    box-sizing: border-box; /* Inclut le padding dans la largeur totale */
}


h2 {
    float:center;
    font-size: 40px;
    margin-bottom: 10px;
    color: #0A2E5C;
    justify-content: center; /* Centre horizontalement */
    align-items: center;
    display: flex;

}
p {
    justify-content: center; /* Centre horizontalement */
    align-items: center;
    display: flex;
    float: center;
    margin-bottom: 20px;
    color:#0A2E5C;
    font-size: 18px;
}
p a{
    color:rgb(159, 14, 86);
}
form {
    display: flex;
    flex-direction: column;
    gap: 15px;
    width: 80%; /* Ajuste la largeur totale du formulaire */
    margin: 0 auto; /* Centre le formulaire horizontalement */
}

label {
    text-align: left;
    color: #0A2E5C;
    
}

input {
    width: 100%; /* Assure que les champs remplissent leur conteneur */
    padding: 10px;
    font-size: 16px; /* Agrandit la taille du texte dans les champs */
    border: 1px solid #ccc;
    border-radius: 5px;
}


input:focus {
    border-color: #0A2E5C;
    outline: none;
 }
 #email, #password , #conf-password{
    width: 100%; /* Assure que le champ utilise toute la largeur du conteneur */
    padding: 15px; /* Augmente la hauteur en ajoutant plus de padding */
    font-size: 18px; /* Agrandit la taille du texte */
    border-radius: 10px;
    background-color: rgb(228, 225, 225);
    box-sizing: border-box; /* Inclut le padding dans la largeur totale */
}

.remember-me input[type="checkbox"] {
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    width: 50px;
    height: 25px;
    background-color: #ccc;
    border-radius: 25px;
    cursor: pointer;
    position: relative;
  }
  .remember-me input[type="checkbox"]:checked {
    background-color: #0A2E5C;
}

  .remember-me input[type="checkbox"]:before {
    content: "";
    position: absolute;
    top: 3px;
    left: 3px;
    width: 20px;
    height: 20px;
    background-color: white;
    border-radius: 50%;
    transition: transform 0.3s ease;
  }
  
  .remember-me input[type="checkbox"]:checked:before {
    transform: translateX(24px);
  }

button {
    background-color:#0A2E5C;
    color: white;
    padding: 10px;
    border: none;
    border-radius: 15px;
    cursor: pointer;
    font-size: 20px;
}

button:hover {
    background-color: #0056b3;
}
